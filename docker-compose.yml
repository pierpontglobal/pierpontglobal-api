version: '3'
services:
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
    - .:/pierpontglobal-api
    environment:
      - DB_HOST='172.18.0.1'
    ports:
    - "3000:3000"